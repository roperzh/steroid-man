<ol>
  <% packages.each do |package| %>
      <li class="result">
        <h3><a href="#"><%= package.get(:name) %></a></h3>
        <p><%= package.short_description %></p>
      </li>
  <% end %>
</ol>
